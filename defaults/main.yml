---
# Set default vars
gitops:
  repo_path: /tmp
  username: admin
  email: ansible_git@ansible.com
  password: "@Mudar"
  protocol: http
  url: gitlab.com
  repo_uri: /estudosdevops/ansible/roles/
  branch: master
  msg: 'added: new Virtual Host by Ansble'

# Only applies if gitops is `true`.
gitops_clone: no
gitops_commit: no
# gitops_push: no
